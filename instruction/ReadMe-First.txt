Follow Instruction Step by Step

1.) Make Database in MySql (DB Name = school);
   - Now Make 2 tabe by using given query
     CREATE TABLE teachers (id INT AUTO_INCREMENT PRIMARY KEY, username VARCHAR(50) NOT NULL, password VARCHAR(255) NOT NULL);

	 CREATE TABLE students (id INT AUTO_INCREMENT PRIMARY KEY, name VARCHAR(100),subject VARCHAR(100),marks INT);
	 
	- Just Run Both Query and Insert Data Manually Otherwise we have one more option I provided sql file just import in database.
	
2.) Download git file and keep inside xamp(any server) inside htdoc folder.

3.) Make User Account manually in database and now ready for use 
